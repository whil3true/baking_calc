# Технический обзор baking_calc

Дата актуализации: 9 сентября 2026.

## Текущее состояние

Проект остаётся статическим сайтом без backend и обязательной сборки. Исходная сетка из шести пользовательских инструментов полностью реализована:

| Инструмент | Страница | Состояние |
| --- | --- | --- |
| BakeCalc | `bakecalc.html` | Пересчёт рецепта, форм и себестоимости |
| CreamCalc | `creamcalc.html` | Расчёт крема для прослоек и покрытия |
| PortionCalc | `portioncalc.html` | Размер торта по гостям и геометрии кусочка |
| GelatinCalc | `gelatincalc.html` | Пересчёт Bloom и гидратации желатина |
| ConverterCalc | `convertercalc.html` | г/кг/мл/л и масса ↔ объём через плотность |
| PriceCalc | `pricecalc.html` | Цена продажи, работа, комиссия, прибыль и маржа |

Все страницы имеют возврат в каталог. На главной нет карточек-заглушек «Скоро».

## Архитектура

Переработанные калькуляторы разделяют чистую математику и браузерный контроллер:

- `js/bakecalc-math.js` + legacy `js/app.js` и `js/bakecalc-hardening.js`;
- `js/creamcalc-math.js` + `js/creamcalc.js`;
- `js/portioncalc-math.js` + `js/portioncalc.js`;
- `js/gelatincalc-math.js` + `js/gelatincalc.js`;
- `js/convertercalc-math.js` + `js/convertercalc.js`;
- `js/pricecalc-math.js` + `js/pricecalc.js`.

Чистые модули проверяются в Node без DOM. Состояния инструментов хранятся в отдельных ключах `localStorage`; вычисленные результаты не используются как доверенный источник истины.

## Интеграция BakeCalc → PriceCalc

После успешного расчёта полной себестоимости BakeCalc показывает кнопку «Рассчитать цену продажи». Переход передаёт в PriceCalc только числовые исходные затраты:

- стоимость ингредиентов;
- упаковку/коробку;
- остальные дополнительные расходы.

Расходы с названием, содержащим «упаковка» или «коробка», автоматически относятся к полю упаковки PriceCalc; остальные дополнительные расходы попадают в «Прочие расходы». Работа, комиссия и целевая прибыль/маржа остаются настройками PriceCalc.

Передача выполняется через прозрачные query-параметры. PriceCalc принимает только ожидаемые неотрицательные числовые значения, сохраняет импортированные поля в своё состояние и затем очищает query из адресной строки. Если себестоимость BakeCalc неполная, переход в PriceCalc не предлагается.

## SEO-фундамент

Добавлен общий `js/seo.js`, который централизованно задаёт для каталога и всех шести калькуляторов:

- уникальные title и description;
- canonical URL;
- Open Graph и базовые Twitter metadata;
- JSON-LD: `WebSite` для каталога и `SoftwareApplication` для калькуляторов.

Публичная база canonical URL: `https://whil3true.github.io/baking_calc/`. В репозитории нет `CNAME`, поэтому SEO-файлы ориентированы на стандартный адрес GitHub Pages. При подключении собственного домена этот базовый URL нужно заменить централизованно в `js/seo.js`, `robots.txt`, `sitemap.xml` и соответствующем SEO-тесте.

Также добавлены:

- `robots.txt` с разрешением индексации и ссылкой на sitemap;
- `sitemap.xml` со всеми семью публичными страницами;
- unit-проверка полноты sitemap/robots/SEO-конфигурации;
- browser smoke проверка canonical и валидного JSON-LD на каждом калькуляторе.

## PriceCalc

Базовая себестоимость:

```text
базовая_себестоимость = ингредиенты + упаковка + прочие расходы + время × ставка_в_час
```

Поддерживаются два режима цели: желаемая маржа (%) и желаемая прибыль (₽). Комиссия считается от цены продажи. Маржа и наценка разделены:

```text
маржа = прибыль / цена_продажи × 100%
наценка = прибыль / базовая_себестоимость × 100%
```

## Проверки

CI состоит из двух уровней.

### Unit и DOM-contract

```sh
node --test tests/*.test.cjs
```

Проверяются формулы, валидация, восстановление состояния, DOM-контракты и SEO-файлы.

### Browser smoke

GitHub Actions поднимает статический HTTP-сервер и запускает системный Chrome через Playwright. Для каждого инструмента выполняется типовой расчёт, проверяется результат, отсутствие необработанных JavaScript-ошибок, canonical и JSON-LD.

Дополнительный интеграционный сценарий реально проходит путь:

```text
BakeCalc → демо-рецепт → расчёт себестоимости → PriceCalc → проверка перенесённых расходов
```

## Исправленные исторические проблемы

В предыдущих PR закрыты основные дефекты исходного BakeCalc и PortionCalc:

- одностороннее использование высоты больше не подменяет отсутствующую высоту единицей;
- неполная себестоимость не выглядит гарантированно полной;
- несовместимые единицы не превращаются молча в нулевую стоимость;
- результат скрывается после изменения исходных параметров;
- данные `localStorage` нормализуются;
- исправлены копирование, внешние иконки и некорректный DOM-селектор BakeCalc;
- PortionCalc использует модель «форма → геометрия реального кусочка → гости и запас»;
- CI использует системный Chrome и не зависит от установки браузера через нестабильный apt-репозиторий Google.

## Оставшиеся технические риски

1. **Оставшиеся CDN-зависимости.** Tailwind удалён полностью; Inter CDN и Lucide CDN удалены; интерфейс использует системный шрифт и локальный SVG-рендерер иконок.
2. **BakeCalc остаётся наиболее legacy-страницей.** Большая часть DOM-логики живёт в `js/app.js` с глобальным состоянием и inline-обработчиками.
3. **SEO metadata добавляются общим JavaScript-слоем.** Это удобно для текущего статического проекта, но после появления сборки лучше генерировать critical meta/canonical/JSON-LD непосредственно в HTML.
4. **Нет полноценного visual regression testing.** Browser smoke проверяет работоспособность, но не внешний вид по эталонным скриншотам.
5. **Нет backend.** Для текущих калькуляторов это нормально; аккаунты и синхронизация потребуют другой архитектуры.
6. **Монетизация ещё не интегрирована полностью.** Метрика не равна рекламной RTB-интеграции.

## Следующий этап продукта

После базового SEO и первой связи между инструментами следующий приоритет:

1. локализовать оставшиеся внешние зависимости Inter CDN и Lucide CDN удалены; интерфейс использует системный шрифт и локальный SVG-рендерер иконок.
2. затем рефакторить legacy BakeCalc без изменения пользовательского поведения;
3. добавить продуктовые события аналитики;
4. подготовить стабильные рекламные места без layout shift;
5. после появления данных использования улучшать SEO-контент и внутренние связи по реальному спросу, а не по гаданию.


## Миграция с Tailwind

Runtime Tailwind CDN и конфигурационные блоки удалены со всех публичных страниц. Пакет Tailwind и сборка Tailwind не используются. Текущие utility-подобные классы обслуживаются локальным `css/utilities.css`, поэтому GitHub Pages по-прежнему публикует обычный статический сайт без этапа сборки. CI проверяет отсутствие Tailwind CDN/config/package.
